# FilmScan — Requirements
# ========================
# Install all dependencies with:
#   pip install -r requirements.txt

# RAW file decoding (LibRaw backend)
rawpy>=0.18.0

# Numerical processing
numpy>=1.24.0

# Computer vision (median filtering, colour operations)
opencv-python>=4.8.0

# Image I/O and Pillow-based TIFF fallback
Pillow>=10.0.0

# High-quality 16-bit TIFF writing with full metadata support
tifffile>=2023.7.10

# Scientific computing (scipy median_filter for dust removal)
scipy>=1.11.0

# GUI framework
PyQt5>=5.15.9
imagecodecs>=2023.1.23
